// Re-exported from @traek/core for backwards compatibility.
// Import directly from '@traek/core' in new code.
export { computeLayout, buildLayoutInput } from '@traek/core';
export type { LayoutInput, NodePosition, LayoutMode, LayoutConfig } from '@traek/core';
