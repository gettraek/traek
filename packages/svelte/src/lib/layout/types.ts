// Re-exported from @traek/core for backwards compatibility.
// Import directly from '@traek/core' in new code.
export type { LayoutMode, NodePosition, LayoutConfig, LayoutInput } from '@traek/core';
export { LAYOUT_MODE_LABELS } from '@traek/core';
