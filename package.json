{
	"name": "traek-monorepo",
	"private": true,
	"type": "module",
	"packageManager": "pnpm@10.29.3",
	"scripts": {
		"dev": "turbo dev --filter=@traek/web --continue && pnpm run turbo watch",
		"watch": "turbo watch",
		"clean": "turbo clean && rm -rf node_modules .svelte-kit build coverage .turbo .pnpm-store dist test-results",
		"build": "turbo build",
		"test": "turbo test",
		"lint": "turbo lint",
		"format": "prettier --write . && turbo format",
		"check": "turbo check"
	},
	"devDependencies": {
		"prettier": "^3.8.1",
		"prettier-plugin-svelte": "^3.4.1",
		"turbo": "^2.3.3",
		"typescript": "^5.9.3"
	}
}
